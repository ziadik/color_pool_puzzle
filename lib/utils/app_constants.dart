class AppConstants {
  static const double navButtonSize = 40;
  static const double navButtonRound = 8;
  static const Duration ballMoveDuration = Duration(milliseconds: 500);
  static const Duration ballCaptureDuration = Duration(milliseconds: 300);
  static const Duration buttonAnimationDuration = Duration(milliseconds: 100);
  static const double defaultPadding = 8;
  static const double smallPadding = 4;
  static const double titleFontSize = 22;
  static const double buttonFontSize = 16;
}